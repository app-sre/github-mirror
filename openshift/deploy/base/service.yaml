---
apiVersion: v1
kind: Service
metadata:
  name: github-mirror
  labels:
    app: github-mirror
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
  selector:
    app: github-mirror
